# Pine Time App Test Environment Configuration

# Enable Demo Mode to bypass database connection
DEMO_MODE=true

# Database Configuration (not used in demo mode but included for completeness)
DATABASE_TYPE=postgresql
POSTGRES_SERVER=localhost
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=pine_time
POSTGRES_SSL_MODE=prefer

# Test User Credentials
TEST_ADMIN_USERNAME=testadmin
TEST_ADMIN_PASSWORD=testadminpassword
TEST_ADMIN_EMAIL=testadmin@example.com
