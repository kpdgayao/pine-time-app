# Pine Time Admin Dashboard - Environment Configuration
# ------------------------------------------
# This file provides example environment variables for development.
# Copy this file to .env (which is gitignored) and modify as needed.

# API Base URL (without trailing slash)
# This is the URL where the backend API is running
# IMPORTANT: Do not include /api/v1 in this URL as it's added automatically
VITE_API_BASE_URL=http://localhost:8000

# Development Mode
# Setting NODE_ENV to development enables additional debug features
# This is automatically set by Vite in development mode
NODE_ENV=development

# API Timeouts (optional - defaults defined in config.ts)
# VITE_DEFAULT_TIMEOUT=10000
# VITE_EXTENDED_TIMEOUT=30000
