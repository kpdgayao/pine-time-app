# CRITICAL: Order matters for these rules

# First rule: Redirect /admin to /admin/ (trailing slash important)
/admin    /admin/   301

# Second rule: SPA fallback for admin dashboard
/admin/*  /admin/index.html  200

# Ensure assets are properly served
/assets/*  /assets/:splat  200
