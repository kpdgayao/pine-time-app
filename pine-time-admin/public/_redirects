# Redirects for subdomain deployment (admin.pinetimeapp.com)

# For SPA routing - all paths should serve index.html
/*    /index.html    200

# Ensure assets are properly served
/assets/*  /assets/:splat  200
